
import 'package:flutter/material.dart';

void main(){
  runApp(
      new Centre(
          child: new Text(
            "hello there",
            textDirection: TextDirection.ltr,
          )
      )
  );
}
